# ── Core Web Framework ──────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0

# ── Voice & Realtime Layer (Component 02) ───────────
aiortc>=1.14.0                # WebRTC: signaling, ICE, DTLS-SRTP, media
deepgram-sdk>=7.0.0           # STT + TTS: streaming via Deepgram (Aura)
numpy>=1.26.0                 # Audio frame processing
aiohttp>=3.9.0                # Async HTTP client for internal API calls

# ── Utilities ───────────────────────────────────────
python-dotenv>=1.0.0
requests>=2.31.0

# ── Data Pipeline (Phase 1–2 scripts) ──────────────
datasets>=2.14.0
pandas>=2.0.0
